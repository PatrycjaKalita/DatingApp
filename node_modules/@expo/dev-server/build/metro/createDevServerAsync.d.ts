/// <reference types="expo__bunyan" />
/// <reference types="node" />
import type Log from '@expo/bunyan';
import http from 'http';
import type MetroConfig from 'metro-config';
declare type RunServerProps = {
    config: MetroConfig.ConfigT;
    logger: Log;
    onReady?: (server: http.Server) => void;
};
export declare function createDevServerAsync(projectRoot: string, { config, logger, onReady }: RunServerProps): Promise<{
    server: http.Server;
}>;
export {};

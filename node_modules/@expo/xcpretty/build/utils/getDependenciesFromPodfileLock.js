"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getDependenciesFromPodfileLock = void 0;
const fs_1 = __importDefault(require("fs"));
const js_yaml_1 = require("js-yaml");
const EXTERNAL_SOURCES_KEY = 'EXTERNAL SOURCES';
function getDependenciesFromPodfileLock(podfileLockPath) {
    let fileContent;
    try {
        fileContent = fs_1.default.readFileSync(podfileLockPath, 'utf8');
    }
    catch (err) {
        return {};
    }
    // Previous portions of the lock file could be invalid yaml.
    // Only parse parts that are valid
    const tail = fileContent.split(EXTERNAL_SOURCES_KEY).slice(1);
    const checksumTail = EXTERNAL_SOURCES_KEY + tail;
    const contents = js_yaml_1.load(checksumTail);
    if (!contents || typeof contents !== 'object') {
        return {};
    }
    return contents[EXTERNAL_SOURCES_KEY] || {};
}
exports.getDependenciesFromPodfileLock = getDependenciesFromPodfileLock;
//# sourceMappingURL=getDependenciesFromPodfileLock.js.map